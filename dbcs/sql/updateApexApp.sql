--
-- Set parameters to allow the import process to load apps from any original workspace and schema
-- into the new target workspace and schema ('Jules').          ===
--
execute SET_APEX_APP_LOAD_PARAMS;